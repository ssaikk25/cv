"""Пакет решения задачи бинарной сегментации области манипуляции.

Модули:
    config  — пути к данным и ограничения конкурса;
    data    — чтение данных, разбиение без утечки, датасет;
    model   — U-Net с шумовым каналом и подсчёт FLOPs;
    metrics — метрика AIC Score;
    train   — обучение и подбор порога;
    predict — инференс на тесте и сборка посылки.
"""
